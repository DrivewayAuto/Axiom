services:
  - type: web
    name: axiom
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
